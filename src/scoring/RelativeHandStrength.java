package scoring;

public class RelativeHandStrength {
	
	//Returns just the difference in category, doesn't take handscore into account
	/*public int compareCategory(HandEvaluator fullHand, HandCategory commCardsCat) {
		int categoryGap; //the difference between the 2 categories of hands
		categoryGap = fullHand.getHandCategory().ordinal() - commCardsCat.handCategory.ordinal();
			return categoryGap;
	}*/

}
